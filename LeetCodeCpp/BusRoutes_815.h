#pragma once

#include <vector>

class BusRoutes_815 {
public:
    int numBusesToDestination(std::vector<std::vector<int>>& routes,
                              int source,
                              int target);
};
