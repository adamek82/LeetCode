#pragma once
#include <vector>

using namespace std;

class TwoSum_1 {
public:
    // Hash-map single-pass solution, O(n) time / O(n) space.
    vector<int> twoSum(const vector<int>& nums, int target);
};
