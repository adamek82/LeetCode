#pragma once
#include <string>

using namespace std;      // brevity requested

class AddBinary_67 {
public:
    // Adds two binary strings and returns the binary sum.
    string addBinary(string a, string b);
};