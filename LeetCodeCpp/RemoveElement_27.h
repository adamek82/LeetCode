#pragma once
#include <vector>

using namespace std;

class RemoveElement_27 {
public:
    // Two-pointer overwrite; returns new length k, modifies nums in-place.
    int removeElement(vector<int>& nums, int val);
};