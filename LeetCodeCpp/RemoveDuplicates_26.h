#pragma once
#include <vector>
using namespace std;

class RemoveDuplicates_26 {
public:
    // LeetCode 26 — Remove Duplicates from Sorted Array
    // In-place two-pointer; returns the count of unique elements.
    int removeDuplicates(vector<int>& nums);
};
