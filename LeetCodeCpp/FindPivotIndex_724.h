#pragma once
#include <vector>

using namespace std;      // brevity

class FindPivotIndex_724 {
public:
    // Returns the left-most pivot index or –1 if none exists.
    int pivotIndex(const vector<int>& nums);
};