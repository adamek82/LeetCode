#pragma once
#include <vector>
#include <unordered_map>

class MajorityElement_169 {
public:
    int majorityElement(std::vector<int>& nums);
    int majorityElementWithHashmap(const std::vector<int>& nums);
};