#pragma once
#include <string>

using namespace std;

class MaximumNumberOfBalloons_1189 {
public:
    // Counts how many full words “balloon” can be built from ‘text’.
    int maxNumberOfBalloons(string& text);
};