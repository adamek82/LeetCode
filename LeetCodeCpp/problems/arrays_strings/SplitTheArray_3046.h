#pragma once
#include <vector>
using namespace std;

class SplitTheArray_3046 {
public:
    // Return true iff we can split nums into two size-n/2 parts,
    // each with all distinct elements.
    bool isPossibleToSplit(vector<int>& nums);
};
