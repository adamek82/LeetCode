#pragma once
#include <vector>

using namespace std;

class BestTimeToBuyAndSellStockII_122 {
public:
    int maxProfit_DailyDiffs(const vector<int>& prices);
    int maxProfit_ValleyPeak(const vector<int>& prices);
};
