#pragma once
#include <string>
using namespace std;

class JewelsAndStones_771 {
public:
    // Returns how many characters in `stones` also appear in `jewels`.
    int numJewelsInStones(string jewels, string stones);
};
