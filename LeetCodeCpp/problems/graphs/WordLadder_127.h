#pragma once

#include <string>
#include <vector>

using namespace std;

class WordLadder_127 {
public:
    int ladderLength(
        const string& beginWord,
        const string& endWord,
        const vector<string>& wordList);
};
