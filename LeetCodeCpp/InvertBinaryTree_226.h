#pragma once

#include "TreeNode.h"

class InvertBinaryTree_226 {
public:
    // Inverts the binary tree rooted at `root` and returns the same root pointer.
    TreeNode<int>* invertTree(TreeNode<int>* root);
};
